# Validate Binary Search Tree (LeetCode 98)

Given the root of a binary tree, determine if it is a valid binary search tree (BST).

## Approach
Use a recursive helper function with lower and upper bounds.
